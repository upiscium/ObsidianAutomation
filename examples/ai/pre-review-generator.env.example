# Copy to /etc/obsidian-ai/pre-review-generator.env in private deployment authority.
# OPENAI_API_KEY is optional for trusted local endpoints and must remain private.
OPENAI_BASE_URL=https://openai-compatible.example.invalid/v1
# OPENAI_API_KEY=replace-in-private-deployment-only
